web:
  image: nginx
  ports:
    - 8080:80
  environment:
    - NGINX_HOST=foobar.com
    - NGINX_PORT=80
  volumes:
    - ./web/html:/usr/share/nginx/html
    - ./web/default.conf:/etc/nginx/conf.d/default.conf